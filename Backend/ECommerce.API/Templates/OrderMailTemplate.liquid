<!DOCTYPE html>
<html>
<head>
    <title>Siparişiniz Alınmıştır! 🎉</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            padding: 20px;
        }
        .container {
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: auto;
        }
        h1 {
            color:#e67e22;
        }
        .order-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        .order-table th, .order-table td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        .order-table th {
            background-color: #e67e22;
            color: white;
        }
        .btn {
            display: inline-block;
            background-color: #e67e22;
            color: white;
            padding: 10px 15px;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <img src="https://i.imgur.com/n4EKdw1.png" alt="Şirket Logosu" style="max-width: 200px;">
        <h1>Siparişiniz Alınmıştır! 🎉</h1>
        <p>Merhaba, siparişiniz başarıyla alınmıştır! Aşağıda detayları görebilirsiniz. 🛍️</p>
        {% for order in orders %}
            <div>
                <h2>Sipariş Numarası: {{ order.OrderNumber }}</h2>
                <table class="order-table">
                    <thead>
                        <tr>
                            <th>Resim</th>
                            <th>Ürün Adı</th>
                            <th>Adet</th>
                             <th>Beden</th>
                             <th>Renk</th>
                            <th>Birim Fiyat</th>
                            <th>Toplam</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for orderItem in order.OrderItems %}
                            <tr>
                                <td>
                                    {% if orderItem.ImageUrl %}
                                        <img src="https://cdn3d.iconscout.com/3d/premium/thumb/product-3d-icon-download-in-png-blend-fbx-gltf-file-formats--tag-packages-box-marketing-advertisement-pack-branding-icons-4863042.png" style="max-width: 50px; ">
                                    {% else %}
                                        Görsel Yok
                                    {% endif %}
                                </td>
                                <td>{{ orderItem.Name }}</td>
                                <td>{{ orderItem.Quantity }}</td>
                                 <td>{{ orderItem.Size }}</td>
                                 <td>{{ orderItem.Color }}</td>
                                <td>{{ orderItem.UnitPrice }} TL</td>
                                <td>{{ orderItem.TotalPrice }} TL</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
                <p><strong>Toplam Sipariş Tutarı: {{ order.TotalPrice }} TL</strong></p>
            </div>
            <hr>
        {% endfor %}
        <a href="#" class="btn">Siparişinizi Takip Edin 🚚</a>
        <p>Teşekkür ederiz! 💖</p>
    </div>
</body>
</html>

