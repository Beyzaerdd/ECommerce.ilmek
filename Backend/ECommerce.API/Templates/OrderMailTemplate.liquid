<!DOCTYPE html>
<html>
<head>
    <title>Siparişiniz Alınmıştır!</title>
    <style>
        body {
            font-family: sans-serif;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            border: 1px solid #ddd;
        }
        th, td {
            padding: 8px;
            border: 1px solid #ddd;
            text-align: left;
        }
        img {
            max-width: 50px;
        }
    </style>
</head>
<body>

 <img src="https://i.imgur.com/kGseOlZ.png" alt="Şirket Logosu" style="max-width: 400px; max-height: 400px;  margin-bottom: 10px;">

    <h1>Siparişiniz Alınmıştır!</h1>

    {% for order in orders %}
        <div style="margin-bottom: 20px;">
            <h2>Sipariş Numarası: {{ order.OrderNumber }}</h2>

            <h3>Ürünler:</h3>
            <table>
                <thead>
                    <tr>
                        <th>Resim</th>
                        <th>Ürün Adı</th>
                        <th>Adet</th>
                        <th>Birim Fiyat</th>
                        <th>Toplam</th>
                    </tr>
                </thead>
                <tbody>
                    {% for orderItem in order.OrderItems %}
                        <tr>
                            <td>
                                {% if orderItem.ImageUrl %}
                                    <img src="{{ orderItem.ImageUrl }}" alt="{{ orderItem.Name }}">
                                {% else %}
                                    Görsel Yok
                                {% endif %}
                            </td>
                            <td>{{ orderItem.Name }}</td>
                            <td>{{ orderItem.Quantity }}</td>
                            <td>{{ orderItem.UnitPrice }} TL</td>
                            <td>{{ orderItem.TotalPrice }} TL</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>

            <p><strong>Toplam Sipariş Tutarı: {{ order.TotalPrice }} TL</strong></p>
        </div>
        <hr>
    {% endfor %}

    <p>Teşekkür ederiz!</p>
</body>
</html>
